# Claude Code — Project Guidelines

## Git 브랜치 전략

- **머지 순서**: `feature/*` → `dev` → `main`
- `feature → dev` 머지는 작업 완료 시 자유롭게 진행
- **`dev → main` 머지는 반드시 사용자의 명시적 요청이 있을 때만 수행**
- main에 직접 커밋/push 금지
